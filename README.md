This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

# README

## Currency exchange widget

To run:

```
yarn install
yarn start
```

Then view in the browser with http://localhost:3000
